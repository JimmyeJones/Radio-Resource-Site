node_modules
.next
dist
# Anchor to the context root so runtime volumes are excluded but bundled
# source data (src/data/exam/*.json) still ships in the image.
/data
/media
.git
.gitignore
*.md
!README.md
.vscode
.idea
.env*
